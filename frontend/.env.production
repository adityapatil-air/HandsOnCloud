# Production API base — points to API Gateway (set at build time by CodeBuild)
# CodeBuild overrides this via the environment or injects it before npm run build
REACT_APP_API_URL=https://replace-with-api-gateway-id.execute-api.ap-south-1.amazonaws.com/prod
